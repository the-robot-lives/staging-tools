#!/usr/bin/env bash
# Deploys all staging services. See docs/prd/stage-and-dev-flow.md
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/../helm-tools/helm-upgrade" --env stage --force "$@"
